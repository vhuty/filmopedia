import { GenreWallpaperPipe } from './genre-wallpaper.pipe';

describe('GenreWallpaperPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreWallpaperPipe();
    expect(pipe).toBeTruthy();
  });
});
